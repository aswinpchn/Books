a, b = 5, 10  # This is a unique way of initialization in python.

# Typical swapping mechanism in other languages
# temp = a
# a = b
# b = temp

a, b = b, a  # Using the trick in first line, we can swap using simple technique.
print(a, b)