# String is an special object in Python, it contains continuous characters.
word = 'Hello!'

word = '.'.join(['ab', 'cd', 'ef'])  # 'ab.cd.ef' is the result
# Join is a method of string. Join's argument should be iterable of strings
# The string whose method is called is inserted in between each given string.
print(word)

